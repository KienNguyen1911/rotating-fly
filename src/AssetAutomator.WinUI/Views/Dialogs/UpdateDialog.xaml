<ContentDialog x:Class="AssetAutomator.WinUI.Views.Dialogs.UpdateDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               Title="🚀 Phiên Bản Mới Đã Sẵn Sàng!"
               PrimaryButtonText="Cập nhật ngay"
               CloseButtonText="Nhắc tôi sau"
               DefaultButton="Primary"
               PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <StackPanel Spacing="14" MinWidth="480">
        <Grid ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Phiên bản hiện tại" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.7" />
                <TextBlock x:Name="TxtCurrentVersion" Text="v1.0.0" Style="{ThemeResource SubtitleTextBlockStyle}" FontWeight="Bold" />
            </StackPanel>

            <FontIcon Grid.Column="1" Glyph="&#xE72A;" FontSize="18" VerticalAlignment="Center" Foreground="LightGreen" />

            <StackPanel Grid.Column="2">
                <TextBlock Text="Phiên bản mới nhất" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.7" />
                <TextBlock x:Name="TxtNewVersion" Text="v1.1.0" Style="{ThemeResource SubtitleTextBlockStyle}" FontWeight="Bold" Foreground="LightGreen" />
            </StackPanel>
        </Grid>

        <TextBlock Text="📝 Nội dung cập nhật (Release Notes):" Style="{ThemeResource BodyStrongTextBlockStyle}" />

        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12">
            <TextBox x:Name="TxtReleaseNotes" Text="Đang tải thông tin cập nhật..." IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" FontFamily="Consolas" MinHeight="140" Background="Transparent" BorderThickness="0" />
        </Border>
    </StackPanel>
</ContentDialog>
