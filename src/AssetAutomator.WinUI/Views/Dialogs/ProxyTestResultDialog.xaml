<ContentDialog x:Class="AssetAutomator.WinUI.Views.Dialogs.ProxyTestResultDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:dialogs="using:AssetAutomator.WinUI.Views.Dialogs"
               Title="Kết Quả Kiểm Tra Proxy"
               CloseButtonText="Đóng"
               DefaultButton="Close">

    <StackPanel Spacing="12" MinWidth="540" MinHeight="300">
        <TextBlock x:Name="TxtSummary" Text="Tổng cộng: 0 ONLINE, 0 OFFLINE" Style="{ThemeResource BodyTextBlockStyle}" Opacity="0.8" />

        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="8">
            <ListView x:Name="LstResults" MaxHeight="320" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="dialogs:ProxyResultModel">
                        <Grid Padding="8,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{x:Bind Proxy}" FontFamily="Consolas" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                            <TextBlock Grid.Column="1" Text="{x:Bind Status}" FontWeight="Bold" Foreground="LightGreen" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="2" Text="{x:Bind Response}" Opacity="0.8" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </StackPanel>
</ContentDialog>
