<ContentDialog x:Class="AssetAutomator.WinUI.Views.Dialogs.VoiceSelectorDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:dialogs="using:AssetAutomator.WinUI.Views.Dialogs"
               Title="Chọn Giọng Đọc (ElevenLabs / Edge-TTS)"
               PrimaryButtonText="Chọn Giọng Này"
               CloseButtonText="Hủy"
               DefaultButton="Primary"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               HorizontalContentAlignment="Stretch"
               VerticalContentAlignment="Stretch"
               PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">1400</x:Double>
        <x:Double x:Key="ContentDialogMinWidth">800</x:Double>
    </ContentDialog.Resources>

    <Grid x:Name="RootGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MinHeight="480" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Top Filters Bar -->
        <Grid x:Name="TopFilterGrid" Grid.Row="0" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtSearch"
                     Grid.Column="0"
                     PlaceholderText="Tìm theo tên hoặc ID..."
                     KeyDown="FilterInput_KeyDown" />
            <TextBox x:Name="TxtLanguage"
                     Grid.Column="1"
                     Width="140"
                     PlaceholderText="Mã ng.ngữ (en, hi, es...)"
                     KeyDown="FilterInput_KeyDown" />
            <ComboBox x:Name="ComboSort"
                      Grid.Column="2"
                      Width="130"
                      SelectedIndex="0"
                      HorizontalAlignment="Stretch"
                      SelectionChanged="Filter_Changed">
                <ComboBoxItem Content="Nổi bật" Tag="trending" />
                <ComboBoxItem Content="Mới nhất" Tag="created_date" />
                <ComboBoxItem Content="Lượt dùng nhiều" Tag="cloned_by_count" />
            </ComboBox>
            <ComboBox x:Name="ComboGender"
                      Grid.Column="3"
                      Width="130"
                      SelectedIndex="0"
                      HorizontalAlignment="Stretch"
                      SelectionChanged="Filter_Changed">
                <ComboBoxItem Content="Tất cả Giới tính" Tag="" />
                <ComboBoxItem Content="Nữ (Female)" Tag="female" />
                <ComboBoxItem Content="Nam (Male)" Tag="male" />
            </ComboBox>
        </Grid>

        <TextBlock Grid.Row="1" Text="Danh sách Giọng đọc khả dụng (AI84 API):" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.7" />

        <!-- Voices ListView & Status Overlay -->
        <Grid Grid.Row="2">
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="8">
                <ListView x:Name="LstVoices" SelectionMode="Single" SelectionChanged="LstVoices_SelectionChanged">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="dialogs:VoiceModel">
                            <Grid Padding="10,8" RowSpacing="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <!-- Left info: Name & VoiceId -->
                                <Grid Grid.Column="0" Grid.Row="0" ColumnSpacing="8" VerticalAlignment="Center" Margin="0,0,12,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" MaxWidth="350" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{x:Bind Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1" Text="{x:Bind VoiceId}" FontFamily="Consolas" FontSize="11" Opacity="0.6" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                                </Grid>

                                <!-- Right info: Language & Category Badges -->
                                <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Horizontal" Spacing="6" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <Border Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4" Padding="6,2">
                                        <TextBlock Text="{x:Bind Language}" FontSize="11" Opacity="0.8" />
                                    </Border>
                                    <Border Background="{ThemeResource SystemAccentColorDark2}" CornerRadius="4" Padding="6,2">
                                        <TextBlock Text="{x:Bind Category}" FontSize="11" Foreground="White" />
                                    </Border>
                                </StackPanel>

                                <!-- Description below -->
                                <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Text="{x:Bind Description}" FontSize="12" Opacity="0.6" TextTrimming="CharacterEllipsis" Margin="0,2,0,0" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>

            <!-- Loading & Error Overlay -->
            <Grid x:Name="OverlayStatus" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="8" Visibility="Collapsed">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12">
                    <ProgressRing x:Name="ProgressLoading" IsActive="True" Width="40" Height="40" VerticalAlignment="Center" />
                    <TextBlock x:Name="TxtStatusText" Text="Đang tải danh sách giọng đọc..." Style="{ThemeResource BodyTextBlockStyle}" HorizontalAlignment="Center" TextWrapping="Wrap" MaxWidth="500" TextAlignment="Center" />
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Pagination Controls -->
        <Grid Grid.Row="3" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                <TextBlock Text="Hiển thị:" VerticalAlignment="Center" FontSize="12" Opacity="0.7" />
                <ComboBox x:Name="ComboPageSize" SelectedIndex="1" SelectionChanged="ComboPageSize_SelectionChanged" Width="80">
                    <ComboBoxItem Content="15" />
                    <ComboBoxItem Content="30" />
                    <ComboBoxItem Content="50" />
                </ComboBox>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button x:Name="BtnPrevPage" Content="◄ Trước" Click="BtnPrevPage_Click" IsEnabled="False" Padding="12,4" />
                <TextBlock x:Name="TxtPageIndex" Text="Trang 1 / 1" VerticalAlignment="Center" FontWeight="SemiBold" Margin="4,0" />
                <Button x:Name="BtnNextPage" Content="Sau ►" Click="BtnNextPage_Click" IsEnabled="False" Padding="12,4" />
            </StackPanel>
        </Grid>

        <!-- Footer status -->
        <Grid Grid.Row="4" Margin="0,4,0,0">
            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <TextBlock Text="Voice ID đã chọn:" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.7" VerticalAlignment="Center" />
                <TextBlock x:Name="TxtSelectedVoiceId" Text="Chưa chọn" FontWeight="Bold" Foreground="{ThemeResource SystemAccentColor}" VerticalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Grid>
</ContentDialog>
