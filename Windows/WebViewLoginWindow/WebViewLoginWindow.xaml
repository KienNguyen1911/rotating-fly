<Window x:Class="AssetAutomator.WebViewLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Đăng nhập tài khoản ChatGPT" Height="650" Width="550"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource CanvasBrush}"
        Icon="pack://application:,,,/Resources/app_logo.png">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource CanvasBrush}" Padding="10" BorderBrush="{StaticResource HairlineBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="TxtStatus" Text="Đang khởi tạo trình duyệt..." Foreground="{StaticResource BodyBrush}" VerticalAlignment="Center" FontSize="12"/>
                <Button Grid.Column="1" Content="Tải lại (Refresh)" Click="BtnRefresh_Click" Padding="10,4" FontSize="11" Height="26"/>
            </Grid>
        </Border>

        <wv2:WebView2 Grid.Row="1" x:Name="WvBrowser" SourceChanged="WvBrowser_SourceChanged"/>
    </Grid>
</Window>
